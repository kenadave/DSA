class Solution {
    public void reverseString(char[] s) {
        int n=s.length;
        int f=0;
        int l=(n-1);
        for(int i=0;i<(n/2);i++){
            char temp=s[i];
            s[i]=s[l];
            s[l]=temp;
            l--;
        }
            
    }
}
